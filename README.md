To run the numerical experiments, open a terminal in the project folder and run:

```bash
python main.py
```

The program will run all three problems with all numerical methods and step sizes. The CSV error tables will be saved in `outputs/tables`, and the figures will be saved in `outputs/figures`.
